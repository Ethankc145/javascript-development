//Fourth Task Loop
const countDigits = (number) => {
    return number.toString().length
}

let num1 = 123
let num2 = 5837547

console.log(`${num1} has ${countDigits(num1)} digits.`);
console.log(`${num2} has ${countDigits(num2)} digits.`);
